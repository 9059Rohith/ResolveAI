# Set this to enable LLM mode. Local mode remains available without a key.
OPENAI_API_KEY=
# Optional bearer token for /v1 endpoints in a public deployment.
APP_API_TOKEN=
# Optional audit location. Vercel automatically uses writable /tmp storage.
AUDIT_LOG_PATH=
